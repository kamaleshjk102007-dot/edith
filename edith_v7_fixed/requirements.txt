# E.D.I.T.H. requirements — all phases

# Phase 1 — AI Brain
requests>=2.31.0

# Phase 2 — WhatsApp
pywhatkit>=5.4

# Phase 3 — Word Documents
python-docx>=1.1.0

# Phase 4 — Wake Word (optional — needs Picovoice account for access key)
# pvporcupine>=3.0.0

# Phase 5 — Agent (no extra deps — uses Ollama + router)

# Phase 6 — System Monitor
psutil>=5.9.0

# Phase 7 — Web Search
# beautifulsoup4 is optional (used for Google News RSS fallback)
beautifulsoup4>=4.12.0
lxml>=4.9.0

# Phase 8 — Weather (no extra deps — uses wttr.in HTTP)

# Phase 9 — Email (stdlib only: smtplib, imaplib)

# Phase 10 — Proactive Background Agent (no extra deps)

# HUD Server
flask>=3.0.0
flask-socketio>=5.3.0

# Voice / TTS (optional)
pyttsx3>=2.90

# Utilities
colorlog>=6.7.0

# Phase 2 — WhatsApp Desktop auto-send
pyautogui>=0.9.54

# Phase 1-EXT — Word / Excel / Dataset
openpyxl>=3.1.0
